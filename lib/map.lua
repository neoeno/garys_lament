local M = {}

-- M.position_is_walkable = function (map, pos)
--     local tile_no = tilemap.get_tile("/" .. map .. "#tilemap", "walkable", pos.tx, pos.ty)
--     return tile_no ~= 0 and tile_no ~= nil
-- end

return M
